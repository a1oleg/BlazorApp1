@page "/counter"
@using BlazorApp1.Data


@foreach (string s in reqFields.Keys)
{
    @s

    <FormsValidation route="DirValues"/>
}
<FormsValidation route="Dirs" OnSubmitCallback="@AddNewDir" />

@code {


    public Dictionary<string, string> reqFields { get; set; } = new Dictionary<string, string>();


    //[Parameter]
    //public EventCallback<string> OnClickCallback { get; set; }

    void AddNewDir(string item)
    {
        reqFields.Add(item, item);
        //OnClickCallback.InvokeAsync();
    }


}


